---
id: C-20260614-0735
type: change
title: CAL调用日志系统-模板+Skill+记忆三件套落地
domain: 系统
problems: [P-20260614-0734]
status: 已结案
baseline_window: C-1724 解决了静态引用（calibration_ref 字段），但无法审计动态使用频次与场景
minimum_effect: 每次建 C 卡引用 CAL 时，该 CAL 卡的调用日志表格自动追加一行；月度 Meta 可直接读日志
confounds: Hermes 在新会话中是否执行日志写入规则；Skill 补丁是否被后续更新覆盖
significance: 方向性
verdict: 有效
calibrated: true
calibration_ref: CAL-20260613-1649
process_captured: true
recorded_by: huohaochen
evaluator: 第二裁判
blind: true
observe_window: 0d
due: 2026-06-14
owner: huohaochen
created: 2026-06-14
tags: [change]
---

# 🔵 CAL 调用日志系统

> [!question] 一句话
> 在 CAL 卡模板、videoloop Skill、Hermes 记忆三处加调用日志机制——每次引用 CAL 时自动追加一行，零额外操作负担。

---

> [!danger] 改动
> **改前**：C 卡引了 CAL，但没有留下「这次调用发生在什么场景」的记录
> **改后**：建 C 卡引用 CAL 时，Hermes 自动往 CAL 卡底部调用日志表格追加一行

---

## ✅ 怎么做

- [x] ① CAL 模板（TEMPLATE-Calibration.md）加调用日志表格 + 防 Goodhart HTML 注释
- [x] ② 现有 CAL 卡追加调用日志段（问题即解药回填 1 行历史记录）
- [x] ③ videoloop Skill 加「Hermes 调用日志写入」规则
- [x] ④ Hermes 记忆合并写入（扫 CAL + 写日志 + 防 Goodhart 一条）
- [x] ⑤ validate_loop.py 全通过（14 张卡 / 0 告警）

---

> [!success] 预测
> 下次建 C 卡引用 CAL 时，CAL 卡调用日志表格自动多一行。月度 Meta 打开 CAL 卡，日志行数 = 动态调用次数。

---

> [!note]- 过程层（复盘时展开）
> **方案选择理由**
> 模块 B 的 B0 明确了静态引用（`calibration_ref` 字段）与动态使用（真正查了尺子做判断）的区别。B1 的方案——复用「建 C 卡前扫 CAL」动作顺手写调用记录——是零额外负担的最优解。B3 轻方案（CAL 卡追加表格）比单开调用事件表更符合「够用即止」原则。
> 
> **赌的假设**
> Hermes 在新会话中会执行调用日志规则。如果不会，需要升级到更硬的机制。
> 
> **标尺**：[[校准卡-20260613-1649]] 问题即解药原则——发现「静态引用不够用」这个缺口本身就是系统在进步。
