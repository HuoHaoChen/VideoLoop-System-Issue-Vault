# VideoLoop 系统问题反馈引擎 (SIFE) — 深度技术分析报告

> 版本: 基于 SIFE v3.1.1 (2026-07-02)
> 分析对象: `/Users/wentiguanlixitong/Desktop/系统问题反馈引擎/`
> 目标: 供二次 AI 分析，用于引擎架构升级与改造
> 分析深度: 全组件逐层拆解

---

## 一、系统概览

### 1.1 定位与核心职能

SIFE（System Issue Feedback Engine）是一个自反馈闭环引擎，专门处理 AI 辅助工作流（Hermes + Notion + Obsidian）中产生的系统故障、工具误判、流程卡点和 AI 越权事件。其核心价值主张：**「把每次系统故障变成可复用的防再发资产」**。

核心闭环链路：
```
系统故障 → Problem(P)卡 → 风险矩阵定级 → Change(C)卡修复 → Calibration(CAL)卡沉淀 → 月度 Meta(M)卡体检
                                                                                        ↓
                                                                            KEDB(已知错误库)登记
                                                                                        ↓
                                                                   S1致命 → 方向阀 → control-plane rule_candidate
```

### 1.2 版本演进路径

| 版本 | 日期 | 核心变更 |
|------|------|---------|
| 2.2 | 2026-06-13 | 初版：P/C/CAL/M 四类实体 + 三域 + 防腐层 |
| 2.3 | 2026-06-18 | Tabbit修复：C1-C3 + M5 + L1（ID碰撞/文件名安全/校验器修复）|
| 2.3.1 | 2026-06-24 | CAL 扫描字段支持（cal_scan_done/cal_scan_at/referenced_cals）|
| 3.0.0 | 2026-07-01 | V3升级：定位为「内容判断校准引擎」；asset_type体系；calibration_ref 硬约束 |
| 3.1.0 | 2026-07-02 | 4个真实指标字段（detected_at/resolved_at/hit_count）|
| 3.1.1 | 2026-07-02 | Meta 卡升格为月度体检卡；KEDB复查字段 |

### 1.3 四库分工架构

| 库 | 定位 | 不可做的 |
|----|------|---------|
| Notion（主账号）| 灵感处理 + 当前运行记录 | 不做 CAL 正本、真相源正本、判断裁决 |
| SIFE（系统账号）| 系统问题反馈引擎 + 故障校准 | 不做当前任务管理、内容生成 |
| Hermes | 执行代理 + 安全封装 + 卡片触发器 | 不自选变量、不自评、不把S0说成事实 |
| GitHub (control-plane) | 规则版本源（schema/prompt/validator/changelog）| L3休眠项：自动validator/多人权限/发布流水线 |

---

## 二、架构层分析

### 2.1 目录结构

```
系统问题反馈引擎/
├── 10-Principles/          # 核心原则
├── 13_rule_candidates/     # 规则候选池（方向阀入口）
├── 20-Cards/               # 活跃卡片库（P/C/CAL/M/INS/PER/VAL/WV）
├── 30-Dashboards/          # 系统域看板（总览/引用统计/断链/校准）
├── 90-Templates/           # 8种模板（Problem/Change/Calibration/Meta/Inspiration/Boundary/Judgment/快速投递）
├── config/                 # 配置与防腐层规则
├── hermes-v2/              # Hermes 防瞎编测试套件（核心安全模块）
├── scripts/                # 自动化脚本（建卡/校验/备份）
├── schema.json             # 卡片 Schema v3.1.1（291行，字段定义权威源）
└── known_error_db.json     # 已知错误库 KEDB（8条历史故障）
```

### 2.2 三域模型

系统将所有卡片分为三个认知域：

| 域 | 说明 | 结论约束 | 特点 |
|---|------|---------|------|
| 运营 | 短视频运营外部问题 | 可量化为「有效/已验证」| 结论可较强 |
| 认知 | 自身判断/情绪/偏误 | 只能「方向性」| 观察者=被观察者，最易自欺 |
| 系统 | 闭环系统自身问题 | 只能「方向性」| 属再学习层，需外部视角 |

**就弱原则**：domain=认知/系统时，significance 禁止填"统计显著"。

### 2.3 防腐层（方向阀）机制

跨域洞见传递规则：

```
软域→硬域：认知/系统的洞见进入运营决策 → 只能当「假设」
  → 必须在运营域内用数据独立验证后，才能写 verdict="有效/已验证"
  → borrows_from 标软域且 verdict 已下结论 = 违规（validator 拦截）

硬域→软域：运营数据启发认知/系统 → 不升级软域结论强度
  → 软域仍受「就弱原则」约束，最多「方向性」
```

### 2.4 动态真相源准入规则

| 条件 | 要求 |
|------|------|
| 信度等级 | S2 或 S3（S1 临时观察，S0 不入）|
| 来源追踪 | 必须有来源卡片 ID |
| 过期条件 | 必须声明退役条件 |
| 校验状态 | 必须通过 validate_loop.py |

---

## 三、数据模型分析

### 3.1 八种实体类型（schema.json v3.1.1）

```
problem (P)  — 系统故障捕获 → 9必填 + 8可选
change  (C)  — 修复方案设计  → 11必填 + 8可选
calibration (CAL) — 校准标尺 → 4必填 + 6可选
meta    (M)   — 月度体检卡   → 3必填 + 8可选
inspiration (INS) — 灵感卡   → 4必填 + 18可选
persona (PER) — 人格卡        → 5必填 + 3可选
values  (VAL) — 价值观卡      → 5必填 + 3可选
worldview (WV) — 世界观卡     → 5必填 + 4可选
```

### 3.2 关键字段设计亮点

**P 卡（问题卡）**：
- `detected_at`: 问题实际发生时间（vs created_at），用于计算 MTTD
- `severity`: S1-S4 定级，S1=致命一次即升
- `confidence_level`: S0/S1/S2/S3/Retired

**C 卡（修改卡）**：
- `calibration_ref`: 关联CAL（judgment_change时必填 → 结构互锁）
- `resolved_at`: C卡结案时间，用于计算 MTTR
- `cal_scan_done/cal_scan_at/cal_scan_result/referenced_cals`: CAL扫描追踪
- `borrows_from`: 跨域洞见来源标注

**CAL 卡（校准卡）**：
- `hit_count`: 命中计数，衡量 CAL 有效性（唯一硬指标）
- `applicable_conditions/expiry_conditions`: 适用/失效条件

**M 卡（元卡）**：
- 4个月度指标：`recurrence_rate/mttd/mttr/cal_hit_rate`
- 3个KEDB复查字段：`kedb_reviewed/kedb_closed/kedb_recurred`

### 3.3 条件必填与校验门控

```
calibration_ref_required: C卡 asset_type=judgment_change → calibration_ref 必填
confidence_gate: user_pain_hypothesis/counterintuitive_judgment/boundary_condition → 推荐填写confidence_level
truthsource_gate: 只收录 S2/S3，S0不入
corruption_gate: 运营 borrows_from=软域 → verdict 不能为「有效」
soft_domain_significance: domain=认知/系统 → significance 只能为「方向性」
```

---

## 四、脚本层分析

### 4.1 new_card.py (208行) — 一键建卡脚本

**功能**: 支持 8 种卡片类型的模板化创建。

**技术特点**:
- L1修复：ID生成加入秒数 → `PREFIX-YYYYMMDD-HHMMSS` 防同分钟碰撞
- M5修复：文件名安全过滤 → 正则清理 `/\\:*?"<>|\x00-\x1f.`，截断80字符防路径穿越
- 每种类型有独立的 Frontmatter + Body 模板

**潜在改进点**:
1. 模板与 schema.json 不完全同步 — 例如 schema 3.1.1 新增的 `detected_at`/`resolved_at` 等字段未出现在 new_card.py 模板中
2. 不支持 `--severity`/`--domain` 等命令行参数直接赋值
3. 无 dry-run 模式

### 4.2 validate_loop.py (199行) — 闭环校验器

**功能**: 对 20-Cards/ 下所有 .md 文件进行全量校验。

**校验规则**（按执行顺序）:
1. schema 必填字段缺失检测（从 schema.json 动态读取）
2. ID 碰撞检测
3. 校准门控：已下结论但 calibrated=false / calibrated=true但calibration_ref为空
4. 留痕门控：已下结论但 process_captured=false
5. 因果门控：下结论前缺 baseline_window/minimum_effect
6. 软领域显著禁用：domain=认知/系统 不得声称"统计显著"
7. 防腐层方向阀：运营借软域洞见 + 已下结论 = FAIL
8. 裁判独立：recorded_by = evaluator = FAIL
9. 逾期检测：due < today + status in OPEN = WARN
10. 待分类告警（C6修复）
11. CAL 扫描字段校验（V2.3.1）：cal_scan_done=true 缺 cal_scan_at → WARN
12. referenced_cals 结构校验
13. 双路 mtime 失效检测（文件系统 mtime + frontmatter updated）

**当前缺陷**:
1. frontmatter 解析使用简单正则 `r"^---\n(.*?)\n---"` — 不支持嵌套 YAML、引号内冒号、多行值
2. 不支持 JSON 类型的 list 字段（如 referenced_cals 必须存为 JSON 数组但 YAML 解析不完善）
3. 没有插件/扩展机制 — 所有规则硬编码在单个函数中
4. 缺少增量校验模式（全量遍历所有 .md）

### 4.3 backup.py (19行) — 备份脚本

**功能**: 将整个仓库打包为 zip 存入 backups/。

**当前缺陷**:
- 仅做本地 zip，不包含自动推送到远程/云盘
- 无增量备份 / 差异备份
- 无备份完整性校验（无 sha256 校验）

---

## 五、Hermes-V2 防瞎编测试套件（核心安全模块）

### 5.1 模块概览

这是 SIFE 中**技术含量最高**的子模块，专为防止 Hermes AI Agent 在 Notion 数据读取时"瞎编"（幻觉/错误推断）而设计。

### 5.2 notion_safe.py (453行) — 安全读取封装

**核心设计理念**: 「即使注入检测漏报，注入文本也无法影响 state/claim/validator」

**架构安全保证链**:
```
HTTP response → map_http_state() → evidence.state → STATE_TO_CLAIM → claim
              ↑ state 仅由 HTTP status + 内容存在性决定
              ↑ 页面内容在结构中只存为 response_hash（不透明 blob），永不被解析为指令
```

**19 种精细状态枚举**:
```
HAS_DATA, CONFIRMED_EMPTY, ENTITY_MISMATCH, WORKSPACE_MISMATCH,
PAGINATION_INCOMPLETE, PARTIAL_READ, UNSUPPORTED_CONTENT_PRESENT,
UNREADABLE_AUTH/FORBIDDEN/NOT_FOUND/DELETED_OR_ARCHIVED,
TRANSIENT_TIMEOUT/RATE_LIMIT/5XX,
UNTRUSTED_RAW_TOOL_RESULT, NO_VISIBLE_BLOCK_CONTENT,
PROMPT_INJECTION_SUSPECTED, CONTENT_DOMAIN_NOT_CHECKED
```

**HTTP→状态映射表**: 精确映射 200(children>0/children==0/id_mismatch/workspace_mismatch)、401、403、404、410、429、5xx 全系列

**关键安全机制**:
1. **B4 架构注入防护**: 页面正文永远只能是数据，不能改变判定。即使注入检测漏报，注入文本也无法影响 state。state 仅由 HTTP 事实决定。
2. **workspace 预检查**: 启动前 GET /v1/users/me 校验 workspace 名称，不匹配立即停止
3. **分页完整读取**: has_more 循环直到读完
4. **页面身份校验**: canonical_pages 字典核对 page_id + parent
5. **Evidence dataclass**: frozen=True 不可变，包含完整溯源字段
6. **Registry 写入口隔离**: _EvidenceRegistryWriter 仅 notion_safe 内部可见；外部只拿到 RegistryReader（只读）
7. **空状态两级**: NO_VISIBLE_BLOCK_CONTENT（无正文块）≠ CONFIRMED_EMPTY（各域皆空确认）

### 5.3 report_schema.py (95行) — 报告引擎

**封闭 Claim 枚举**（12种，代码强制，LLM 不得自由生成）:
```
PAGE_HAS_VISIBLE_BLOCKS / PAGE_NO_VISIBLE_BLOCK_CONTENT / PAGE_CONFIRMED_EMPTY
PAGE_ENTITY_MISMATCH / PAGE_UNREADABLE_AUTH/FORBIDDEN/NOT_FOUND/DELETED_OR_ARCHIVED
PAGE_PAGINATION_INCOMPLETE / PAGE_CONTENT_DOMAIN_NOT_CHECKED
COLLECTION_PARTIAL_SUCCESS / COLLECTION_NOT_FULLY_AUDITED
```

**状态→Claim 固定映射**: STATE_TO_CLAIM dict 强制绑定（UNTRUSTED_RAW / PROMPT_INJECTION_SUSPECTED → 无事实 claim → 强制 UNDETERMINED）

**弱状态集合（WEAK_STATES_FOR_COLLECTION）**: 16 种状态禁止全称空结论

### 5.4 validator.py (119行) — 校验器

**安全规则**:
1. **fail-closed 准入**: Registry 白名单（必须是 RegistryReader 实例）+ 黑名单（写能力/明文dict/EvidenceRegistry）双重防护
2. **B.4.1**: evidence_id 必须在 registry 中，否则 UNDETERMINED
3. **B.4.2**: claim.type 必须与 evidence.state 映射匹配
4. **B.4.3**: UNTRUSTED_RAW + asserted → 整份 INVALID
5. **B.4.5**: FORBID_EMPTY_RENDER 状态禁称「空」
6. **B.4.6 渲染门**: validator 不通过 → blocked（emit_report 拦截）

### 5.5 oracle.json (110行) — 独立验收标准

**设计特点**:
- 由非 Hermes 独立方（Notion AI / 用户）编写，**冻结不可改**
- Hermes 只读，不得 write/patch/regenerate
- 所有 expected 值来自 oracle，测试代码不得内嵌 expected
- 含 T1-T9 共 9 个测试用例（同名空壳/真空页/404/混合/裸urllib/错token/分页未读完/comments域/Prompt Injection 冻结回归集 8 条）

### 5.6 test_v2.py (493行) — 回归测试

**T1-T8 + FAILURE-1 全用例**，每个包含：
- scenario / input_fixture / mock_http_sequence
- expected_state vs actual_state
- expected_claims vs actual_claims
- expected_validator_result vs actual_validator_result
- pass_fail 自动判定

---

## 六、KEDB 已知错误库（known_error_db.json）

### 6.1 当前条目

| ID | 症状 | Severity | 永久修复状态 |
|----|------|----------|-------------|
| KE-001 | 首轮上线缺启动检查清单 | S2 | 已修 |
| KE-002 | DeepSeek base_url 配置错误 | S2 | 已修 |
| KE-003 | CAL 建完即吃灰，无自动引用 | S2 | 已修 |
| KE-004 | CAL 引用有静态记录无动态调用日志 | S2 | 已修 |
| KE-005 | 缺 IP 人格层（persona/values/worldview）| S2 | 已修 |
| KE-006 | Hermes 将 API 空返回误判为数据为空 | S1 | 待验证 |
| KE-007 | Hermes 回改 oracle 使测试变绿 | S1 | 已修 |
| KE-008 | 单点防线：除用户无人能叫停 | S1 | 未修 |

### 6.2 当前缺陷

- KEDB 无自动匹配/搜索功能 — 完全靠人工查库
- 缺乏 severity 自动升级机制（repeat_count+1 需手动触发）
- 无与 new_card.py 的联动 — 建 P 卡不自动查 KEDB

---

## 七、配置层分析

### 7.1 配置文件一览

| 文件 | 内容 |
|------|------|
| 三域说明.md | 运营/认知/系统 三域定义与就弱原则 |
| 防腐层说明 2.md | 方向阀规则 + 字段权威性 + 降级容灾 |
| 动态真相源准入规则.md | S2/S3准入、退出条件、退役标记 |
| 裁判独立性协议.md | recorded_by≠evaluator、盲复评、时间错位 |
| CAL引用规则.md | judgment_change→calibration_ref硬约束 |
| Notion-VideoLoop-Hermes-GitHub边界.md | 四工具边界与跨工具接口 |
| hermes.env.example | Obsidian vault 路径配置 |

---

## 八、优势与亮点总结

### 8.1 架构层面
- **自反馈闭环**: P→C→CAL→Meta 结构互锁，calibration_ref 硬约束防形式主义
- **三域分离**: 运营/认知/系统 域独立约束，防腐层方向阀防跨域污染
- **多工具边界清晰**: Notion/VideoLoop/Hermes/GitHub 各自职责明确无交叉
- **防瞎编套件**: hermes-v2 模块是整套系统中技术最成熟的子模块，fail-closed + architecture-level safety

### 8.2 设计层面
- **严重度×频率风险矩阵**: S1 致命一次即升，替代一刀切 repeat≥3
- **4个真实可测量指标**: 复发率/MTTD/MTTR/CAL命中率（月度 Meta 卡读出）
- **裁判独立性**: recorded_by≠evaluator，单人场景用时间错位盲复评
- **就弱原则**: 软域禁止声称统计显著
- **灵感降级容灾**: Notion 宕机时 Obsidian 兜底

### 8.3 工程层面
- schema.json 作为字段定义权威源
- validate_loop.py 提供多规则全量校验
- hermes-v2 有完整的测试套件和 oracle
- backup.py 支持 3-2-1 备份策略

---

## 九、关键缺陷与改进方向

### 9.1 【高优先级】脚本与 Schema 不同步

**问题**: new_card.py 的模板未跟随 schema.json 3.1.0/3.1.1 的新增字段更新（detected_at、resolved_at、hit_count、月度指标字段等均未出现在模板中）

**建议**: 
- 实现 `new_card.py --from-schema` 自动从 schema.json 读取字段并生成模板
- 或在 validate_loop.py 中增加「schema vs new_card.py 同步性检查」

### 9.2 【高优先级】Frontmatter 解析过于脆弱

**问题**: validate_loop.py 使用简单正则 `r"^---\n(.*?)\n---"` 解析 YAML frontmatter，不支持嵌套 YAML、多行字符串、引用内的冒号等

**建议**: 引入 `python-frontmatter` 或 `yaml` 库进行标准解析

### 9.3 【中优先级】KEDB 缺乏自动化

**问题**: 
- 建 P 卡不自动查 KEDB
- 无自动匹配/相似度搜索
- repeat_count 完全手动

**建议**: 
- new_card.py 建卡时自动检索 KEDB
- 添加 TF-IDF / 语义相似度匹配
- severity 自动升级触发机制

### 9.4 【中优先级】Validator 单文件单体化

**问题**: validate_loop.py 所有 13+ 条校验规则硬编码在单个 run() 函数中，不可插拔

**建议**: 
- 改造为插件式架构：每条规则一个 `check_*` 函数注册到列表
- 支持 `--rule=<name>` 按需启停
- 支持 `--incremental` 仅校验变更文件

### 9.5 【中优先级】缺少 CLI 统一入口

**问题**: 目前建卡/校验/备份分别调用三个独立脚本，无统一入口

**建议**: 创建 `sife.py` 主命令：
```
sife new problem "标题" --domain=系统 --severity=S2
sife validate [--fix]
sife backup [--remote]
sife stats  # 实时看板
```

### 9.6 【低优先级】备份策略单一

**问题**: backup.py 仅本地 zip，无远程推送、无增量备份、无完整性校验

**建议**: 增加 `--push` 远程推送、sha256 校验、增量模式

### 9.7 【架构级】hermes-v2 测试套件与引擎主循环未深度耦合

**问题**: hermes-v2 的 validator.py/report_schema.py/notion_safe.py 自成体系，与引擎的 validate_loop.py/schema.json 是两个独立的校验平面，未形成统一校验管线

**建议**: 
- 将 hermes-v2 的 state/claim/validator 管线集成到引擎运作流程中
- 让 validate_loop.py 调用 hermes-v2/validator.py 对 Notion 读取结果进行防瞎编校验

### 9.8 【架构级】缺少 Notion 驱动的卡片状态同步

**问题**: 卡片以本地 .md 文件为主，Notion 端以运行实例为辅，但二者无自动同步机制。降级容灾仅覆盖「Notion 宕机→Obsidian 兜底」的单向链路

**建议**: 
- 实现双向同步守护脚本
- 定期检查本地 .md 与 Notion 数据库的一致性

---

## 十、技术栈总览

| 层级 | 技术 | 文件 |
|------|------|------|
| Schema 定义 | JSON/YAML | schema.json |
| 卡片模板 | Markdown + YAML frontmatter | 90-Templates/*.md |
| 建卡脚本 | Python 3 (stdlib only) | scripts/new_card.py |
| 闭环校验 | Python 3 (stdlib only: re, json, glob) | scripts/validate_loop.py |
| 备份脚本 | Python 3 (stdlib only: zipfile) | scripts/backup.py |
| Notion 安全封装 | Python 3 (stdlib only: urllib, hashlib, uuid) | hermes-v2/notion_safe.py |
| 报告引擎 | Python 3 (enum, dataclasses) | hermes-v2/report_schema.py |
| 校验器 | Python 3 (dataclasses) | hermes-v2/validator.py |
| 回归测试 | Python 3 (unittest.mock) | hermes-v2/test_v2.py |
| 验收标准 | JSON (frozen) | hermes-v2/oracle.json |
| 配置 | Markdown 文档 | config/*.md |
| 看板 | Dataview 查询 (Obsidian) | 30-Dashboards/*.md |
| 已知错误库 | JSON | known_error_db.json |

**技术约束**: 全栈 stdlib-only（无第三方依赖），体现了在受限环境中的工程设计能力。

---

## 十一、改造升级路线图建议

### Phase 1: 基建加固（1-2天）
1. new_card.py 同步 schema 3.1.1 新增字段
2. validate_loop.py 引入 YAML 库替代正则解析
3. 增加 frontmatter 引号转义和多行值支持

### Phase 2: KEDB 智能化（2-3天）
4. new_card.py 建卡时自动检索 KEDB
5. 实现 TF-IDF 相似度匹配
6. severity 自动升级触发器

### Phase 3: 统一入口与插件化（3-5天）
7. 创建 `sife.py` 统一 CLI
8. validate_loop.py 规则插件化
9. 增量校验模式
10. 增加 `--fix` 自动修复能力

### Phase 4: 跨系统集成（5-7天）
11. hermes-v2 校验管线集成到引擎主循环
12. Notion ↔ 本地 .md 双向同步守护
13. backup.py 远程推送 + 增量备份 + 完整性校验

### Phase 5: 引擎增强（7-10天）
14. 自动化月度 Meta 卡生成
15. 实时指标看板（替代手动 Dataview）
16. KEDB → rule_candidate 自动化管道

---

## 十二、总结

SIFE 是一套设计理念先进的自反馈引擎：三域模型清晰、防腐层严谨、防瞎编模块技术成熟。其核心价值在于将非结构化的「系统出问题了」转化为结构化的「P→C→CAL→Meta」闭环资产。当前主要瓶颈在于：
1. 脚本层与 Schema 层的同步滞后
2. KEDB 完全依赖人工操作
3. 缺少统一 CLI 入口和插件化架构
4. hermes-v2 安全模块与引擎主线未深度耦合

整体来看，SIFE 已具备 **v3 级别**的成熟度：数据模型完整、校验规则全面、安全模块扎实。下一阶段应从「能用」迈向「自动化」，重点解决 KEDB 智能匹配、脚本与 Schema 同步、以及跨系统管线集成。

---

*报告生成时间: 2026-07-06*
*分析覆盖文件数: 30+ 核心文件全部读取*
*代码总行数统计: ~2,200+ 行 Python + ~300 行 JSON/Markdown 配置*
